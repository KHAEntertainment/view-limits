---
name: update-providers
description: Refresh view-limits account status live from the providers.
---

!`"${CLAUDE_PLUGIN_ROOT}/bin/vl.js" refresh 2>&1`

The table above is live. Call out any `exhausted` route and when it resets.
